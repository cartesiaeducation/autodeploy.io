<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title" id="modal">Regenerate Repository Key</h4>
</div>
<div class="modal-body">
    <p>
        We have generated a SSH key your your new project. You need to add the following key in your GitHub / BitBucket deploy keys :
    </p>

    <pre>{{ project.authentificationRepository.sshPublic }}</pre>

    <div class="margin-top-30 check-permissions">
        <button class="btn btn-info btn-lg btn-block uppercase" data-href="{{ path('app_projects_check_permissions', {id: project.id}) }}">Check Permissions</button>
    </div>
    <div class="margin-top-30 masked permissions-success">
        <a class="btn btn-success btn-lg btn-block uppercase" class="btn btn-default" href="{{ path('app_project_authentification_list', {id: project.id}) }}">Permissions OK</a>
    </div>
</div>
